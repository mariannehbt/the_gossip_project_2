
<!DOCTYPE html>
<html>
<head>
	<title>The Gossip Project - Home</title>
</head>
<body>
	<h1>Bienvenue sur The Gossip Project !</h1>

	<% @gossips.each do |gossip| %>
	<div class="row">
		<div class="col-sm-6">
			<div class="card">
				<div class="card-body">
					<h5 class="card-title"><%= gossip.title %></h5>
					<p class="card-text">par : <%= gossip.user.first_name %></p>
					<a href=<%= "/gossip/" + gossip.id.to_s %> class="btn btn-primary"><%= "Gossip n°" + gossip.id.to_s %></a>
				</div>
			</div>
		</div>
	</div>
	<% end %>

</body>
</html>
